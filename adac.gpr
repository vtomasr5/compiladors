project Adac is

   for Source_Dirs use ("src", "lib");
   for Object_Dir use "build";
   for Main use ("prova_tnoms.adb", "prova_tsimbols.adb");
   for Exec_Dir use "bin";

   package Builder is
      for Default_Switches ("ada") use ("-g");
   end Builder;

end Adac;

