project Adac is

   for Source_Dirs use ("src", "lib");
   for Object_Dir use "build";
   for Main use ("prova_tsimbols.adb", "prova_tnoms.adb");
   for Exec_Dir use "bin";

   package Builder is
      for Default_Switches ("ada") use ("-gnatc");
   end Builder;

   package Compiler is
   end Compiler;

end Adac;

